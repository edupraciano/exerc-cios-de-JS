function action() {
  let num = Number(prompt("Digite um número."));
  let ant = num - 1;
  let sus = num + 1;
  alert(`Antes de ${num}, temos o número ${ant}.\nDepois de ${num}, temos o múmero ${sus}.`);
}
